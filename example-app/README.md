# @capgo/capacitor-stripe-terminal Demo

Exercise Stripe Terminal discovery and reader connection. Requires a backend that provides connection tokens.

## Run

```bash
bun install
bun run start
```

For native testing:

```bash
bun run build
bunx cap sync
bunx cap open ios   # or android
```
